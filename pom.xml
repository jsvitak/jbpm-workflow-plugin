<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>1.447</version>
    </parent>

    <groupId>org.jenkinsci.plugins.jbpm</groupId>
    <artifactId>jbpm-workflow-plugin</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>hpi</packaging>
    <name>jBPM workflow plugin</name>
    <url>https://github.com/jsvitak/jbpm-workflow-plugin</url>
    <description>This plugin allows you to define an order of launching your Jenkins jobs by using a jBPM workflow. The jBPM 5 is a flexible lightweight business process suite based on the BPMN 2.0 standard.</description>
    
    <licenses>
        <license>
            <name>GNU General Public License, Version 3</name>
            <url>http://www.gnu.org/copyleft/gpl.html</url>
        </license>    
    </licenses>
    
    <developers>
        <developer>
            <id>jsvitak</id>
            <name>Jiri Svitak</name>
            <email>jsvitak@redhat.com</email>
        </developer>
    </developers>
    
    <scm>
        <connection>scm:git:git://github.com/jsvitak/jbpm-workflow-plugin.git</connection>
        <developerConnection>scm:git:git@github.com:jsvitak/jbpm-workflow-plugin.git</developerConnection>
        <url>http://github.com/jsvitak/jbpm-workflow-plugin</url>
    </scm>

    <properties>
        <janino.version>2.5.15</janino.version>
        <jbpm.version>5.4.0-SNAPSHOT</jbpm.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.jbpm</groupId>
            <artifactId>jbpm-bpmn2</artifactId>
            <version>${jbpm.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jbpm</groupId>
            <artifactId>jbpm-human-task-hornetq</artifactId>
            <version>${jbpm.version}</version>
        </dependency>
        <dependency>
            <groupId>janino</groupId>
            <artifactId>janino</artifactId>
            <version>${janino.version}</version>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </repository>
        <repository>
            <id>jboss-public-repository-group</id>
            <name>JBoss Public Repository Group</name>
            <url>http://repository.jboss.org/nexus/content/groups/public/</url>
            <layout>default</layout>
            <releases>
                <enabled>true</enabled>
                <updatePolicy>never</updatePolicy>
            </releases>
            <snapshots>
                <enabled>true</enabled>
                <updatePolicy>daily</updatePolicy>
            </snapshots>
        </repository>
      </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>

</project>
